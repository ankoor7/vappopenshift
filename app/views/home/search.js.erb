// AJAX call to update events on the home page
$('#search_events').html('<%= escape_javascript render partial: "shared/event_summary", locals: {events: @search_events} unless @search_events.nil? %>');
$('#search_events_paginator').html('<%= escape_javascript(paginate(@search_events, :remote => true, :param_name => "events_page").to_s) unless @search_events.nil? %>');
