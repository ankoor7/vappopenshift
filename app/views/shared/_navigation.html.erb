    <!-- Static navbar -->
    <div class="navbar navbar-default navbar-static-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "Vapp", root_path, class: "navbar-brand" %>
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-right">
              <%= search_form_for @q, url: search_path, html: {method: :post, class: "navbar-form navbar-left", role: "search"} do |f| %>
                <div class="form-group">
                <%= f.text_field :causes_or_group_name_or_name_cont, class: "form-control input-sm", placeholder: 'Search' %>
                </div>
                <button type="submit" class="btn btn-sm">Submit</button>
              <% end %>
            <% if current_user %>
              <li><%= link_to "Dashboard", dashboard_path %></li>
              <li><%= link_to "Account", edit_user_registration_path %></li>
              <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
            <% else %>
              <li><%= link_to "Register", new_user_registration_path, class: "active" %></li>
              <li><%= link_to "Sign in", new_user_session_path%></li>
              <li><%= link_to "Forgot your password?", new_user_password_path%></li>
            <% end %>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>


